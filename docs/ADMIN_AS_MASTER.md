# 👨‍💼 Админ работает от имени мастера

## Описание функционала

Администратор может работать с заявками от имени мастера тремя способами:
1. **В рабочей группе мастера (обычный режим)** - автоматическое определение мастера по группе
2. **В рабочей группе мастера (анонимный режим)** ⭐ **НОВОЕ!** - работа с включенной анонимностью админов Telegram
3. **Через личный кабинет** - ручной выбор действий "за мастера"

---

## 🔐 Работа в анонимном режиме (НОВОЕ!)

### Что это?

Telegram позволяет админам групп публиковать сообщения **анонимно** (от имени группы). Когда эта функция включена, бот видит все сообщения от `@GroupAnonymousBot` вместо реального пользователя.

### Преимущества анонимного режима

✅ **Конфиденциальность** - личные данные админа не видны в группе  
✅ **Единообразие** - все действия выглядят как от имени группы  
✅ **Безопасность** - личный аккаунт админа защищен  
✅ **Удобство** - не нужно переключаться между аккаунтами

### Как включить анонимность в группе?

#### Шаг 1: Откройте настройки группы
1. Откройте нужную рабочую группу мастера в Telegram
2. Нажмите на название группы вверху

#### Шаг 2: Настройте права админа
1. Перейдите в раздел **"Администраторы"**
2. Выберите нужного админа из списка
3. Найдите опцию **"Remain Anonymous"** (Оставаться анонимным)
4. **Включите** эту опцию ✅

#### Шаг 3: Проверьте
- После включения все ваши сообщения в группе будут от имени `@GroupAnonymousBot`
- Бот автоматически определит мастера по группе

### Как работает бот с анонимным админом?

```
Админ нажимает кнопку в группе (анонимно)
↓
Бот получает callback от @GroupAnonymousBot (ID: 1087968824)
↓
Система определяет мастера по work_chat_id группы
↓
Выполняет действие от имени найденного мастера
↓
Все изменения числятся за мастером этой группы
✓ Готово!
```

### Пример использования (анонимный режим)

```
У мастера "Дисп" есть рабочая группа (ID: -1001234567890)
↓
Админ включил анонимность в этой группе
↓
В группу приходит заявка #123
↓
Админ (анонимно) нажимает "✅ Принять заявку"
↓
Бот:
  1. Видит callback от @GroupAnonymousBot
  2. Находит мастера по work_chat_id = -1001234567890
  3. Принимает заявку от имени мастера "Дисп"
  4. Отправляет уведомление мастеру "Дисп"
✓ Заявка принята мастером "Дисп"
```

---

## Работа в рабочей группе мастера (обычный режим)

### Как это работает

Когда администратор находится в рабочей группе мастера (например, в группе мастера "Дисп"), он может использовать обычные кнопки мастера:
- ✅ Принять заявку
- 🏠 Я на объекте
- 💰 Завершить
- ⏳ Длительный ремонт

### Логика работы

1. Система определяет, что пользователь - администратор (по роли)
2. Находит мастера по `work_chat_id` рабочей группы
3. Выполняет действие от имени найденного мастера
4. Все изменения числятся за мастером этой группы

### Пример использования (обычный режим)

```
Админ находится в группе мастера "Дисп"
↓
Видит заявку #123 с кнопкой "✅ Принять заявку"
↓
Нажимает кнопку
↓
Система:
  - Определяет админа по telegram_id
  - Находит мастера по work_chat_id группы
  - Принимает заявку от имени мастера
↓
Заявка принята мастером "Дисп" ✓
```

---

## Работа через личный кабинет

### Как это работает

В личном кабинете админа есть дополнительные кнопки для действий "за мастера":
- 🎭 Принять за мастера
- 🎭 На объекте за мастера
- 🎭 Завершить за мастера
- 🎭 DR за мастера

### Логика работы

1. Админ открывает заявку в своем личном кабинете
2. Видит дополнительные кнопки с пометкой "за мастера"
3. Нажимает нужную кнопку
4. Действие выполняется от имени мастера, которому назначена заявка

---

## Технические детали

### Константы

```python
# app/core/constants.py
GROUP_ANONYMOUS_BOT_ID = 1087968824  # ID бота для анонимных админов
```

### Функция определения мастера

```python
# app/handlers/group_interaction.py
async def get_master_from_callback(callback: CallbackQuery, db: Database):
    """
    Получение мастера из callback (поддержка анонимных админов)
    
    Returns:
        Tuple[Master | None, bool]: (master, is_anonymous_admin)
    """
    user_id = callback.from_user.id
    
    if user_id == GROUP_ANONYMOUS_BOT_ID:
        # Анонимный админ - ищем мастера по ID группы
        master = await db.get_master_by_work_chat_id(callback.message.chat.id)
        return master, True
    else:
        # Обычный пользователь или админ
        master = await db.get_master_by_telegram_id(user_id)
        
        if not master:
            # Проверяем, админ ли это
            user = await db.get_user_by_telegram_id(user_id)
            if user and user.role == UserRole.ADMIN:
                master = await db.get_master_by_work_chat_id(callback.message.chat.id)
        
        return master, False
```

### Обработчики

Все групповые обработчики обновлены для поддержки анонимных админов:
- `callback_group_accept_order` - принятие заявки
- `callback_group_refuse_order` - отклонение заявки
- `callback_group_onsite_order` - отметка "на объекте"
- `callback_group_complete_order` - завершение заявки
- `callback_group_dr_order` - длительный ремонт

---

## Уведомления

### Мастер получает уведомление

Когда админ (обычный или анонимный) выполняет действие, мастер получает уведомление:

```
🔔 Уведомление для мастера "Дисп"

Админ принял заявку #123 от вашего имени

📋 Детали:
• Тип техники: Стиральная машина
• Адрес: ул. Примерная, д. 1
• Клиент: Иван Иванов
• Телефон: +7 900 123-45-67
```

### Диспетчер получает уведомление

Диспетчер также получает информацию о действиях в группе.

---

## Логирование

Все действия админа от имени мастера логируются:

```python
logger.info(f"Анонимный админ работает в группе мастера {master.get_display_name()}")
logger.info(f"Админ {user.first_name} работает в группе мастера {master.get_display_name()}")
```

В базу данных добавляется audit log:
```python
await db.add_audit_log(
    user_id=callback.from_user.id,  # ID админа или GroupAnonymousBot
    action="ACCEPT_ORDER_GROUP",
    details=f"Accepted order #{order_id} in group"
)
```

---

## Безопасность

### Проверки прав

1. **Проверка мастера**: система находит мастера по `work_chat_id` группы
2. **Проверка заявки**: проверяется, что заявка назначена найденному мастеру
3. **Проверка группы**: действие должно выполняться в рабочей группе мастера

### Что НЕ может сделать анонимный админ

❌ Работать в группе, для которой не настроен мастер  
❌ Принимать заявки, назначенные другому мастеру  
❌ Выполнять действия вне рабочей группы

---

## FAQ

### Как узнать, включена ли анонимность?

Если ваши сообщения в группе отображаются от имени `@GroupAnonymousBot` - анонимность включена.

### Будет ли работать без анонимности?

Да! Функционал "админ от имени мастера" работает в обоих режимах:
- **С анонимностью** (от @GroupAnonymousBot)
- **Без анонимности** (от вашего аккаунта)

### Может ли обычный мастер работать анонимно?

Нет. Анонимность работает только для админов групп. Мастера должны работать от своего аккаунта.

### Что если у группы нет настроенного мастера?

Бот покажет ошибку:
```
❌ Не удалось определить мастера для этой группы.
Убедитесь, что у мастера настроена эта группа как рабочая.
```

Админ должен настроить рабочую группу для мастера через команду `/setworkgroup`.

---

## Версия

**Добавлено в версии:** v1.3.1  
**Дата:** 16.10.2025  
**Статус:** ✅ Работает в production
