# Руководство по поиску заказов

## Обзор

В боте реализован функционал умного поиска заказов по номеру телефона, адресу клиента или ID заказа. Система автоматически определяет тип поиска на основе введенных данных. Эта функция доступна администраторам и диспетчерам.

## Доступ к функции поиска

1. В главном меню нажмите кнопку **"🔍 Поиск заказов"**
2. Введите поисковый запрос — система сама определит тип поиска

## Умный поиск (Unified Search)

Система автоматически анализирует ваш запрос и определяет, что вы ищете:

### 1. Поиск по ID заказа 🔢

Введите номер заказа:
- `123` - найдет заказ #123
- `4567` - найдет заказ #4567

**Примечание:** Если заказ с таким ID не найден, система автоматически попробует найти этот номер в адресах (например, как номер дома).

### 2. Поиск по телефону 📞

Введите номер телефона в любом формате:
- `+79123456789`
- `89123456789`
- `79123456789`
- `8 (912) 345-67-89`
- `+7 (912) 345-67-89`

Система автоматически:
- Нормализует номер к единому формату
- Находит все заказы этого клиента

### 3. Поиск по адресу 🏠

Введите адрес или его часть (минимум 3 символа):
- `Москва` - найдет все заказы в Москве
- `ул. Ленина` - найдет все заказы на улице Ленина
- `Ленина 15` - найдет адрес с улицей Ленина и домом 15
- `д. 15` или просто `15` - найдет все заказы с номером дома 15

**Особенности:**
- Поиск нечувствителен к регистру
- Можно вводить частичные совпадения
- Система распознает и расширяет сокращения (ул. → улица, д. → дом)

## Как система определяет тип поиска

**Автоматическое определение работает по следующим правилам:**

1. **Если запрос содержит буквы** → поиск по адресу
2. **Если запрос похож на телефон** (11 цифр, начинается с 7 или 8) → поиск по телефону
3. **Если запрос содержит пробелы или знаки** (например "15 а", "42-1") → поиск по адресу
4. **Если только цифры:**
   - 1-4 цифры: пробуем найти как ID заказа, если не найден → ищем в адресах
   - 5-6 цифр: пробуем найти как ID заказа, если не найден → ищем в адресах
   - 7-9 цифр: если разумный ID (<1 миллиона) → ищем заказ, иначе → адрес
   - 10+ цифр: сразу поиск по адресу

## Результаты поиска

Результаты отображаются в удобном формате с информацией о:

- **ID заказа** - уникальный номер заказа
- **Телефон клиента** - отформатированный номер
- **Адрес** - полный адрес клиента
- **Имя клиента** - ФИО клиента
- **Тип техники** - что ремонтируется
- **Описание проблемы** - краткое описание (до 100 символов)
- **Статус заказа** - текущий статус с эмодзи
- **Мастер** - назначенный мастер (если есть)
- **Дата создания** - когда была создана заявка

## Особенности

### Нормализация телефонов

Система автоматически приводит номера телефонов к единому формату:
- Убирает все символы кроме цифр
- Заменяет `8` на `7` для российских номеров
- Добавляет `7` для 10-значных номеров

### Нормализация адресов

- Приводит к нижнему регистру
- Убирает лишние пробелы
- Удаляет знаки препинания в конце

### Удаление дубликатов

При поиске по нескольким критериям система автоматически удаляет дубликаты заказов.

## Права доступа

- **Администраторы** - полный доступ ко всем функциям поиска
- **Диспетчеры** - полный доступ ко всем функциям поиска
- **Мастера** - доступ к поиску отсутствует
- **Неизвестные пользователи** - доступ к поиску отсутствует

## Технические детали

### Файлы реализации

- `app/services/order_search.py` - основной сервис поиска
- `app/handlers/order_search.py` - обработчики команд
- `app/states.py` - состояния FSM для поиска
- `app/keyboards/inline.py` - клавиатуры для интерфейса
- `app/keyboards/reply.py` - кнопки в главном меню

### Поддерживаемые базы данных

Функционал работает как с ORM (SQLAlchemy), так и с legacy SQLite базой данных.

### Производительность

- Поиск по телефону использует точное совпадение (быстрый)
- Поиск по адресу использует LIKE с индексами (средняя скорость)
- Результаты сортируются по дате создания (новые первые)

## Примеры использования

### Поиск конкретного заказа по ID

1. Нажмите "🔍 Поиск заказов"
2. Введите: `1234`
3. Получите заказ #1234

### Поиск всех заказов клиента по телефону

1. Нажмите "🔍 Поиск заказов"
2. Введите: `+7 (912) 345-67-89` или `79123456789`
3. Получите список всех заказов этого клиента

### Поиск заказов по адресу

1. Нажмите "🔍 Поиск заказов"
2. Введите: `Центральный район` или `ул. Ленина` или `Ленина 15`
3. Получите все заказы по этому адресу

### Поиск заказов в конкретном доме

1. Нажмите "🔍 Поиск заказов"
2. Введите: `Ленина 15` (с названием улицы)
3. Или просто: `15` (найдет все адреса с номером 15)
4. Получите заказы в этом доме

## Устранение неполадок

### Заказы не найдены

- Проверьте правильность ввода телефона/адреса
- Убедитесь, что заказы не удалены из системы
- Попробуйте поиск по частичному совпадению

### Ошибки при поиске

- Проверьте подключение к базе данных
- Убедитесь, что у вас есть права доступа
- Обратитесь к администратору системы
