# Тестирование Handlers в Реальных Условиях

## 📋 Чек-лист для проверки handlers

### 1. Проверка Dependency Injection

#### ✅ Автоматическая проверка
```bash
python scripts/test_handlers_realtime.py
```

Скрипт проверит:
- Работу DI middleware
- Подключение к БД
- Регистрацию handlers
- Анализ логов на ошибки

#### ✅ Ручная проверка

1. **Проверка логов на DI инъекции:**
```bash
# Windows PowerShell
Select-String -Path "logs/bot.log" -Pattern "DependencyInjectionMiddleware|db injected" | Select-Object -Last 10
```

2. **Проверка отсутствия прямых созданий Database():**
```bash
# Должно вернуть 0 результатов
Select-String -Path "app/handlers/master.py" -Pattern "Database\(\)" | Measure-Object
```

### 2. Тестирование через Telegram

#### Для мастера:

1. **Проверка "Мои заявки":**
   - Отправьте `/start` в группе мастера
   - Нажмите "📋 Мои заявки"
   - ✅ Должен показать список заявок
   - ✅ Проверьте логи: должен быть вызов `btn_my_orders` с `db: Database`

2. **Просмотр заявки:**
   - Выберите заявку из списка
   - ✅ Должна открыться детальная информация
   - ✅ Проверьте логи: `callback_view_order_master` с DI

3. **Принятие заявки:**
   - Нажмите "✅ Принять заявку"
   - ✅ Заявка должна быть принята
   - ✅ Проверьте логи: `callback_accept_order` с DI

4. **Завершение заявки:**
   - Нажмите "✅ Завершить заявку"
   - Введите сумму
   - ✅ Заявка должна быть завершена
   - ✅ Проверьте логи: `callback_complete_order` с DI

5. **Статистика:**
   - Нажмите "📊 Моя статистика"
   - ✅ Должна показаться статистика
   - ✅ Проверьте логи: `btn_my_stats` с DI

#### Для диспетчера:

1. **Создание заявки:**
   - Нажмите "➕ Создать заявку"
   - Заполните все поля
   - ✅ Заявка должна быть создана
   - ✅ Проверьте логи на отсутствие ошибок

2. **Назначение мастера:**
   - Выберите заявку
   - Нажмите "👨‍🔧 Назначить мастера"
   - ✅ Мастер должен быть назначен
   - ✅ Мастер должен получить уведомление

#### Для администратора:

1. **Управление мастерами:**
   - Проверьте все функции админ-панели
   - ✅ Все должно работать без ошибок

### 3. Мониторинг в реальном времени

#### Просмотр логов в реальном времени:

```bash
# Windows PowerShell
Get-Content "logs/bot.log" -Wait -Tail 50
```

#### Фильтрация по handlers:

```bash
# Только вызовы handlers
Select-String -Path "logs/bot.log" -Pattern "app\.handlers" | Select-Object -Last 20

# Только ошибки
Select-String -Path "logs/bot.log" -Pattern "ERROR|Exception" | Select-Object -Last 20

# DI инъекции
Select-String -Path "logs/bot.log" -Pattern "DependencyInjectionMiddleware" | Select-Object -Last 10
```

### 4. Проверка работы в групповых чатах

#### Важно проверить:

1. ✅ Handlers работают в групповых чатах
2. ✅ Уведомления приходят в правильные чаты
3. ✅ FSM состояния работают корректно
4. ✅ Нет конфликтов между пользователями

### 5. Проверка производительности

#### Мониторинг времени выполнения:

```bash
# Поиск медленных операций (>1 секунды)
Select-String -Path "logs/bot.log" -Pattern "Processed in.*[1-9]\.[0-9]+s" | Select-Object -Last 10
```

### 6. Проверка ошибок

#### Типичные проблемы:

1. **Database() создается напрямую:**
   - ❌ `db = Database()`
   - ✅ `db: Database` в параметрах handler

2. **connect/disconnect в handlers:**
   - ❌ `await db.connect()` / `await db.disconnect()`
   - ✅ Убрать, используется DI

3. **Ошибки DI:**
   - Проверьте, что `DependencyInjectionMiddleware` зарегистрирован в `bot.py`
   - Проверьте порядок middleware

### 7. Автоматизированное тестирование

#### Запуск unit тестов:

```bash
pytest tests/ -v
```

#### Запуск интеграционных тестов:

```bash
pytest tests/integration/ -v
```

### 8. Чек-лист после рефакторинга

- [ ] Все handlers используют `db: Database` из DI
- [ ] Нет прямых созданий `Database()`
- [ ] Нет `connect()/disconnect()` в handlers
- [ ] Логи показывают успешную работу DI
- [ ] Все функции работают в Telegram
- [ ] Нет ошибок в логах
- [ ] Производительность не ухудшилась

## 🔍 Отладка проблем

### Проблема: Handler не получает db

**Решение:**
1. Проверьте, что handler имеет параметр `db: Database`
2. Проверьте регистрацию `DependencyInjectionMiddleware` в `bot.py`
3. Проверьте порядок middleware (DI должен быть после RoleCheck)

### Проблема: Ошибки в логах

**Решение:**
1. Проверьте логи на наличие ошибок
2. Убедитесь, что БД доступна
3. Проверьте права доступа к файлам

### Проблема: Handlers не работают

**Решение:**
1. Проверьте регистрацию router в `app/handlers/__init__.py`
2. Проверьте декораторы handlers (`@router.message`, `@router.callback_query`)
3. Проверьте фильтры и условия

## 📊 Метрики успеха

После рефакторинга DI должны быть:
- ✅ 0 прямых созданий `Database()` в handlers
- ✅ 100% handlers используют DI
- ✅ 0 ошибок связанных с подключением к БД
- ✅ Время ответа handlers не увеличилось
