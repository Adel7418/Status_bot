# Документация: Аналитическая панель парсера

## Обзор

Аналитическая панель парсера предоставляет детальную статистику о работе автоматического парсера заявок из Telegram. Система собирает метрики о каждой попытке парсинга и предоставляет визуализацию данных для анализа эффективности.

## Установка

### 1. Миграция базы данных

Примените миграцию для создания таблицы аналитики:

```bash
.venv/bin/alembic upgrade head
```

Это создаст таблицу `parser_analytics` со следующей структурой:

- `id` - уникальный идентификатор
- `message_id` - ID сообщения в Telegram
- `group_id` - ID группы-источника
- `success` - успешность парсинга (boolean)
- `error_type` - тип ошибки при неудаче
- `parsed_equipment_type` - распознанный тип техники
- `parsed_address` - распознанный адрес
- `parsed_phone` - распознанный телефон
- `confirmed` - подтверждено ли пользователем
- `created_order_id` - ID созданной заявки
- `processing_time_ms` - время обработки в миллисекундах
- `created_at` - время создания записи

### 2. Проверка

После миграции система автоматически начнет собирать статистику при каждой попытке парсинга.

## Использование

### Команда /parser_stats

Доступна для ролей: **Admin**, **Dispatcher**

```
/parser_stats
```

### Основные метрики

#### 📈 Сегодня

- **Всего** - количество попыток парсинга
- **Успешно** - успешно распарсенные сообщения
- **Ошибок** - неудачные попытки
- **Успех** - процент успешности с цветовым индикатором
- **Подтверждено** - количество подтвержденных заявок

#### 📅 За неделю / За месяц

Аналогичные метрики за расширенные периоды.

#### ⚡ Средняя скорость

Среднее время обработки одного сообщения в миллисекундах.

### Визуализация

#### Цветовые индикаторы успешности

- 🟢 **80-100%** - отличная работа
- 🟡 **60-79%** - хорошо
- 🟠 **40-59%** - требует внимания
- 🔴 **0-39%** - критично

#### Гистограмма типов техники

```
🔧 Топ типов техники (неделя)
├ ████████ Стиральная машина: 35
├ ██████░░ Телевизор: 28
├ ████░░░░ Холодильник: 18
└ ██░░░░░░ Посудомоечная машина: 8
```

Unicode блоки (█ ░) показывают относительное количество.

#### Анализ ошибок

```
⚠️ Типы ошибок (неделя)
├ missing_fields: 8
├ invalid_format: 4
```

Показывает распределение типов ошибок для выявления проблемных мест.

### Интерактивные кнопки

#### 📈 График за неделю / 📊 График за месяц

Показывает временную линию парсингов по дням:

```
📈 График за 7 дней

30.11.2025
██████████ 15 (🟢 85.7%)
✅12 ❌3 ☑️10

29.11.2025
████████░░ 12 (🟡 75.0%)
✅9 ❌3 ☑️7
```

Где:

- **Бар** - объем парсингов
- **Процент** - успешность
- **✅** - успешные
- **❌** - неудачные
- **☑️** - подтверждённые

#### 🔄 Обновить

Обновляет статистику до актуальных данных.

## Типы ошибок

Система отслеживает следующие типы ошибок:

| Тип ошибки | Описание |
|------------|----------|
| `missing_fields` | Отсутствуют обязательные поля (адрес, описание) |
| `invalid_format` | Некорректный формат данных (например, телефон) |
| `validation_error` | Ошибка валидации Pydantic |

## API сервиса

### ParserAnalyticsService

```python
from app.services.parser_analytics import ParserAnalyticsService

analytics = ParserAnalyticsService(db.session_factory)
```

#### track_parse_event()

Записывает событие парсинга:

```python
await analytics.track_parse_event(
    message_id=12345,
    group_id=-100123456789,
    success=True,
    parsed_equipment_type="Стиральная машина",
    parsed_address="ул. Ленина 1",
    parsed_phone="+79001234567",
    processing_time_ms=150,
)
```

#### mark_confirmed()

Отмечает подтверждение/отклонение:

```python
await analytics.mark_confirmed(
    message_id=12345,
    confirmed=True,
    created_order_id=42,
)
```

#### get_stats()

Получить агрегированную статистику:

```python
stats = await analytics.get_stats(period_days=7)
# Возвращает:
# {
#     "total_parses": 87,
#     "successful_parses": 75,
#     "failed_parses": 12,
#     "success_rate": 86.2,
#     "confirmed": 68,
#     "rejected": 5,
#     "pending_confirmation": 2,
#     "confirmation_rate": 90.6,
#     "avg_processing_ms": 142.5,
#     "error_breakdown": {...},
#     "equipment_breakdown": {...}
# }
```

#### get_timeline()

Получить временную линию:

```python
timeline = await analytics.get_timeline(days=7)
# Возвращает список:
# [
#     {
#         "date": "30.11.2025",
#         "total": 15,
#         "successful": 12,
#         "failed": 3,
#         "confirmed": 10,
#         "success_rate": 85.7
#     },
#     ...
# ]
```

## Интеграция

Аналитика автоматически интегрирована в:

1. **parser_integration.py** - отслеживание парсинга
2. **confirmation_service.py** - отслеживание подтверждений

### Точки трекинга

```python
# При успешном парсинге
await self.analytics_service.track_parse_event(
    message_id=message_id,
    group_id=self.group_id,
    success=True,
    # ... parsed data
)

# При ошибке парсинга
await self.analytics_service.track_parse_event(
    message_id=message_id,
    group_id=self.group_id,
    success=False,
    error_type=parse_result.status,
)

# При подтверждении
await self.analytics_service.mark_confirmed(
    message_id=order.message_id,
    confirmed=True,
    created_order_id=new_order.id,
)
```

## Мониторинг и оптимизация

### Ключевые показатели

**Успешность парсинга** должна быть > 80%

- Если ниже, проверьте типичные ошибки
- Возможно требуется улучшение словаря сокращений

**Скорость обработки** должна быть < 500ms

- Если выше, проверьте производительность БД
- Рассмотрите индексацию

**Коэффициент подтверждения** должен быть > 85%

- Если ниже, парсер может распознавать неправильные данные
- Требуется уточнение правил парсинга

### Рекомендации

1. **Еженедельный обзор** - проверяйте статистику раз в неделю
2. **Анализ ошибок** - фокусируйтесь на топ-3 типах ошибок
3. **Тренды** - отслеживайте изменения success_rate
4. **Обратная связь** - используйте отклонённые заявки для улучшения парсера

## Примеры использования

### Просмотр статистики за сегодня

```
/parser_stats
```

Основные метрики отображаются сразу.

### Анализ недельного тренда

1. `/parser_stats`
2. Нажать "📈 График за неделю"
3. Просмотреть изменения по дням

### Выявление проблемных типов техники

Проверьте раздел "🔧 Топ типов техники" - если какой-то тип доминирует при низкой успешности, возможно требуется улучшение парсинга именно для него.

## Troubleshooting

### Статистика не собирается

- Проверьте, что миграция применена: `alembic current`
- Убедитесь, что ParserIntegration запущен
- Проверьте логи на ошибки

### Неверные данные

- Убедитесь, что часовой пояс настроен правильно
- Проверьте синхронизацию времени сервера

### Низкая производительность

- Добавьте индексы на часто используемые поля
- Рассмотрите архивацию старых данных (> 6 месяцев)

## Дальнейшее развитие

Возможные улучшения:

1. **Экспорт данных** - выгрузка статистики в CSV/Excel
2. **Алерты** - уведомления при падении успешности
3. **A/B тестирование** - сравнение разных версий парсера
4. **Машинное обучение** - анализ паттернов для улучшения точности
5. **Дашборд** - веб-интерфейс с графиками
