# ЭТАП 3: Pydantic схемы для валидации (ЗАВЕРШЁН)

**Дата:** 27 января 2025
**Длительность:** ~20 минут
**Статус:** ✅ ЗАВЕРШЁН

## Выполненные задачи

### 1. Создан файл schemas.py

**Файл:** `app/services/telegram_parser/schemas.py`

**Назначение:** Валидация и типизация данных парсера с использованием Pydantic 2.10.3.

### 2. Реализованные Pydantic модели

#### Модель 1: OrderParsed

**Назначение:** Представление распарсенной заявки из Telegram-группы.

**Поля:**

```python
class OrderParsed(BaseModel):
    equipment_type: str          # Тип оборудования (обязательно)
    problem_description: str     # Описание проблемы (обязательно)
    address: str                 # Адрес клиента (обязательно)
    client_name: str             # Имя клиента (default="Клиент")
    phone: str | None            # Телефон (опционально)
    scheduled_time: str | None   # Время выезда (опционально)
    raw_message: str             # Исходное сообщение (обязательно)
    message_id: int              # ID сообщения в Telegram (обязательно)
```

**Валидаторы:**

1. **validate_equipment_type()** — проверка что тип оборудования не пустой
2. **validate_problem_description()** — проверка что описание не пустое
3. **validate_address()** — проверка что адрес не пустой
4. **validate_phone()** — проверка что телефон содержит минимум 10 цифр (если указан)

**Ограничения полей (Field):**

- `equipment_type`: min_length=1, max_length=100
- `problem_description`: min_length=1, max_length=500
- `address`: min_length=1, max_length=200
- `client_name`: max_length=100
- `phone`: max_length=20
- `scheduled_time`: max_length=100

**Пример данных (json_schema_extra):**

```python
{
    "equipment_type": "Стиральная машина",
    "problem_description": "Не крутит барабан, шумит при отжиме",
    "address": "ул. Ленина 5, кв. 10",
    "client_name": "Клиент",
    "phone": "+79001234567",
    "scheduled_time": "завтра к 14:00",
    "raw_message": "С/м не крутит барабан. ул. Ленина 5-10. +79001234567 завтра к 14:00",
    "message_id": 12345
}
```

#### Модель 2: ParseResult

**Назначение:** Результат парсинга сообщения (success/failure).

**Поля:**

```python
class ParseResult(BaseModel):
    success: bool                                      # Успешность парсинга
    status: Literal["success", "missing_fields",       # Статус парсинга
                    "invalid_format", "error"]
    data: OrderParsed | None                           # Распарсенные данные (если success=True)
    error_message: str | None                          # Сообщение об ошибке (если success=False)
    missing_fields: list[str]                          # Список пропущенных полей
    parsed_at: datetime                                # Время парсинга
```

**Валидаторы:**

1. **validate_data_with_success()** — проверка консистентности:
   - Если `success=True`, то `data` должен быть заполнен
   - Если `success=False`, то `data` должен быть `None`

**Возможные статусы:**

- `success` — успешный парсинг
- `missing_fields` — отсутствуют обязательные поля (адрес, оборудование)
- `invalid_format` — некорректный формат данных
- `error` — ошибка при парсинге

**Примеры данных (json_schema_extra):**

**Пример 1: Успешный парсинг**

```python
{
    "success": True,
    "status": "success",
    "data": {
        "equipment_type": "Стиральная машина",
        "problem_description": "Не крутит барабан",
        "address": "ул. Ленина 5, кв. 10",
        "client_name": "Клиент",
        "phone": "+79001234567",
        "scheduled_time": "завтра к 14:00",
        "raw_message": "С/м не крутит. ул. Ленина 5-10. +79001234567",
        "message_id": 12345
    },
    "error_message": None,
    "missing_fields": []
}
```

**Пример 2: Парсинг с ошибкой**

```python
{
    "success": False,
    "status": "missing_fields",
    "data": None,
    "error_message": "Не удалось определить адрес",
    "missing_fields": ["address"]
}
```

#### Модель 3: ConfirmationData

**Назначение:** Данные для подтверждения создания заявки через inline-кнопки.

**Поля:**

```python
class ConfirmationData(BaseModel):
    message_id: int                      # ID исходного сообщения в Telegram
    parsed_order: OrderParsed            # Распарсенная заявка для подтверждения
    confirmation_message_id: int | None  # ID сообщения с кнопками подтверждения
    created_at: datetime                 # Время создания запроса
```

**Использование:**

Эта модель будет использоваться для хранения данных при отображении кнопок подтверждения:

```
📋 Создать заявку?

🔧 Тип: Стиральная машина
❗ Проблема: Не крутит барабан
📍 Адрес: ул. Ленина 5, кв. 10
📞 Телефон: +79001234567

[✅ Да] [❌ Нет]
```

**Пример данных (json_schema_extra):**

```python
{
    "message_id": 12345,
    "parsed_order": {
        "equipment_type": "Стиральная машина",
        "problem_description": "Не крутит барабан",
        "address": "ул. Ленина 5, кв. 10",
        "client_name": "Клиент",
        "phone": "+79001234567",
        "scheduled_time": None,
        "raw_message": "С/м не крутит. ул. Ленина 5-10. +79001234567",
        "message_id": 12345
    },
    "confirmation_message_id": 12346
}
```

### 3. Обновлён __init__.py

Добавлен экспорт Pydantic схем:

```python
from .schemas import ConfirmationData, OrderParsed, ParseResult

__all__ = [
    # ... предыдущие экспорты
    "OrderParsed",
    "ParseResult",
    "ConfirmationData",
]
```

## Технические детали

### Использование Pydantic 2.10.3

Все модели используют современный синтаксис Pydantic v2:

1. **field_validator** — декоратор для валидации полей
2. **Field()** — с параметрами description, min_length, max_length, default
3. **Config.json_schema_extra** — примеры для JSON Schema
4. **Literal types** — для строгой типизации статусов

### Type Hints

Полная типизация с Python 3.11+ синтаксисом:

```python
phone: str | None              # Union через |
missing_fields: list[str]      # Списки через []
data: OrderParsed | None       # Опциональные объекты
```

### Валидация данных

**Автоматическая валидация:**
- Длина строк (min_length, max_length)
- Типы данных (str, int, datetime)
- Обязательность полей (...)

**Кастомная валидация:**
- Проверка непустых строк (после strip)
- Проверка минимального количества цифр в телефоне (10+)
- Консистентность success/data в ParseResult

### Примеры использования

#### Пример 1: Создание OrderParsed

```python
from app.services.telegram_parser.schemas import OrderParsed

order = OrderParsed(
    equipment_type="Стиральная машина",
    problem_description="Не крутит барабан",
    address="ул. Ленина 5, кв. 10",
    phone="+79001234567",
    raw_message="С/м не крутит. ул. Ленина 5-10. +79001234567",
    message_id=12345
)

# client_name автоматически = "Клиент"
print(order.client_name)  # → "Клиент"
```

#### Пример 2: Валидация с ошибкой

```python
from pydantic import ValidationError

try:
    order = OrderParsed(
        equipment_type="",  # ОШИБКА: пустое значение
        problem_description="Не работает",
        address="ул. Ленина 5",
        raw_message="...",
        message_id=123
    )
except ValidationError as e:
    print(e)
    # → ValueError: Тип оборудования не может быть пустым
```

#### Пример 3: Создание ParseResult

```python
from app.services.telegram_parser.schemas import ParseResult, OrderParsed

# Успешный парсинг
result = ParseResult(
    success=True,
    status="success",
    data=order,  # OrderParsed объект
    missing_fields=[]
)

# Ошибка парсинга
result_error = ParseResult(
    success=False,
    status="missing_fields",
    data=None,
    error_message="Не удалось определить адрес",
    missing_fields=["address"]
)
```

#### Пример 4: ConfirmationData

```python
from app.services.telegram_parser.schemas import ConfirmationData

confirmation = ConfirmationData(
    message_id=12345,
    parsed_order=order,  # OrderParsed объект
    confirmation_message_id=12346
)

# created_at автоматически = datetime.now()
print(confirmation.created_at)
```

## Структура файлов после этапа

```
telegram_repair_bot/
├── app/
│   └── services/
│       └── telegram_parser/
│           ├── __init__.py               # Обновлён (+ экспорт схем)
│           ├── equipment_dict.py         # Этап 2
│           ├── patterns.py               # Этап 2
│           └── schemas.py                # NEW этот этап
└── docs/
    └── implementation/
        ├── stage1_infrastructure.md
        ├── stage2_patterns.md
        └── stage3_schemas.md             # NEW этот отчет
```

## Следующие шаги

**ЭТАП 4:** Реализация OrderParserService
- `app/services/telegram_parser/parser_service.py`
  - Класс `OrderParserService`
  - Метод `parse_message()` — парсинг текста в OrderParsed
  - Интеграция с equipment_dict, patterns, schemas

**Статус:** Готов к началу 🟢

## Примечания

1. ✅ **Pydantic v2** — используется современный синтаксис (field_validator, Field, Config)
2. ✅ **Type Safety** — полная типизация всех полей и методов
3. ✅ **Валидация** — автоматическая + кастомная валидация критических полей
4. ✅ **Примеры** — json_schema_extra содержит реалистичные примеры данных
5. ✅ **Консистентность** — validate_data_with_success проверяет согласованность success/data
6. 📝 **Документация** — все модели и поля задокументированы через description

## Проверка выполнения

- [x] Создан файл `schemas.py`
- [x] Реализована модель `OrderParsed` с валидаторами
- [x] Реализована модель `ParseResult` с валидаторами
- [x] Реализована модель `ConfirmationData`
- [x] Обновлён `__init__.py` с экспортом схем
- [x] Все модели типизированы и задокументированы
- [x] Отчёт создан

---

**Автор:** Claude Code Sonnet
**Время выполнения:** ~20 минут
**Следующий этап:** ЭТАП 4 (OrderParserService — логика парсинга)
