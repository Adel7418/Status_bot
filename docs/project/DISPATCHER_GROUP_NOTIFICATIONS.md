# 📢 Настройка уведомлений для диспетчеров

Данная инструкция поможет настроить автоматические уведомления о важных событиях (например, восстановление отклонённых заявок) для диспетчеров.

---

## 🎯 Как работают уведомления?

При восстановлении отклонённой заявки администратором, система автоматически отправляет уведомления:

### 📱 Личные уведомления (основной способ)
- **Автоматически** отправляются всем диспетчерам из списка `DISPATCHER_IDS` (уже настроено в `.env`)
- Каждый диспетчер получает личное сообщение от бота
- **Не требует дополнительной настройки** - работает из коробки

### 📢 Уведомления в группу (опционально)
- Дополнительно можно настроить отправку в общую группу диспетчеров
- Удобно для централизованного лога событий
- Требует настройки `DISPATCHER_GROUP_ID`

---

## ✅ Базовая настройка (уже работает!)

Если в вашем `.env` файле указаны `DISPATCHER_IDS`, уведомления уже работают:

```bash
# Telegram ID диспетчеров (через запятую)
DISPATCHER_IDS=123456789,987654321
```

При восстановлении заявки каждый диспетчер получит личное сообщение от бота. **Никаких дополнительных действий не требуется!**

---

## 📋 Дополнительная настройка группы (опционально)

### 1. Создайте группу в Telegram (если ещё нет)

1. Откройте Telegram
2. Создайте новую группу: **"Диспетчеры - Уведомления"** (или любое другое название)
3. Добавьте в группу всех диспетчеров и администраторов

### 2. Добавьте бота в группу

1. Перейдите в настройки группы → Участники
2. Нажмите "Добавить участника"
3. Найдите вашего бота по имени (которое вы задали в @BotFather)
4. Добавьте бота в группу
5. **Важно:** Сделайте бота администратором группы (чтобы он мог отправлять сообщения)

### 3. Получите ID группы

Есть несколько способов получить ID группы:

#### Способ 1: Через @userinfobot

1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в вашу группу
2. Бот автоматически отправит информацию о группе
3. Скопируйте значение **Chat ID** (например: `-1001234567890`)
4. Удалите @userinfobot из группы (больше не нужен)

#### Способ 2: Через @RawDataBot

1. Добавьте бота [@RawDataBot](https://t.me/RawDataBot) в вашу группу
2. Бот отправит JSON с информацией о группе
3. Найдите значение поля `"id"` в разделе `"chat"` (например: `-1001234567890`)
4. Удалите @RawDataBot из группы

#### Способ 3: Через API бота (для продвинутых)

1. Отправьте любое сообщение в группу
2. Перейдите по ссылке: `https://api.telegram.org/bot<ВАШ_BOT_TOKEN>/getUpdates`
3. Найдите `"chat":{"id": -1001234567890}` в ответе
4. Скопируйте значение `id`

### 4. Добавьте ID группы в конфигурацию

Откройте ваш `.env` файл (или `env.city1`/`env.city2` для multibot режима) и добавьте:

```bash
# ID группы диспетчеров для уведомлений
DISPATCHER_GROUP_ID=-1001234567890
```

**Важно:** ID группы всегда начинается с минуса (`-`)!

### 5. Перезапустите бота

```bash
# Для локальной разработки
python bot.py

# Для Docker
docker-compose restart
```

---

## ✅ Проверка работы

После настройки:

1. Перейдите в бот
2. Найдите любую отклонённую заявку через поиск
3. Нажмите "♻️ Восстановить заявку" (только для администраторов)
4. Подтвердите восстановление

**Результат:** В группу диспетчеров должно прийти уведомление:

```
♻️ Заявка восстановлена

📋 Заявка #123
👤 Клиент: Иван Иванов
📱 Телефон: +7 (900) 123-45-67
🔧 Техника: Холодильник
📝 Проблема: Не холодит

Статус: REFUSED → NEW
Восстановлена администратором
```

---

## ❓ Частые вопросы

### Уведомления не приходят. Что делать?

1. **Проверьте, что бот является администратором группы**
   - Откройте информацию о группе
   - Перейдите в Администраторы
   - Убедитесь, что бот в списке

2. **Проверьте правильность ID группы**
   - ID должен начинаться с минуса
   - Проверьте, что нет лишних пробелов в `.env` файле

3. **Проверьте логи бота**
   ```bash
   # Для Docker
   docker logs telegram_repair_bot

   # Для локального запуска
   cat logs/bot.log
   ```

   Ищите строки типа:
   - `Уведомление о восстановлении заявки #123 отправлено в группу диспетчеров` - успех
   - `Ошибка при отправке уведомления в группу` - ошибка

### Можно ли отключить уведомления?

Да! Просто оставьте поле `DISPATCHER_GROUP_ID` пустым или удалите его из `.env`:

```bash
# Уведомления отключены
DISPATCHER_GROUP_ID=
```

### Можно ли настроить несколько групп?

В текущей версии поддерживается только одна группа для уведомлений. Если нужно несколько групп, используйте multibot режим (каждый бот может иметь свою группу).

---

## 🔐 Безопасность

- Не публикуйте ID группы в открытых источниках
- Регулярно проверяйте список участников группы
- Удаляйте ботов-помощников (@userinfobot, @RawDataBot) после получения ID

---

## 📚 Дополнительная информация

- [Документация Telegram Bot API](https://core.telegram.org/bots/api)
- [Руководство по multibot режиму](../deployment/MULTI_BOT_DEPLOY.md)
- [Общая документация проекта](../README.md)

---

**Дата создания:** 2025-11-26
**Версия:** v2.10.0
