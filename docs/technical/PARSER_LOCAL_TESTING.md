# Локальное тестирование парсера

Пошаговое руководство для проверки парсера заявок в боте локально.

## Шаг 1: Получить Telegram API credentials

1. Перейдите на https://my.telegram.org
2. Войдите с вашим номером телефона
3. Нажмите "API development tools"
4. Создайте приложение (любое название, например "Parser Test")
5. Скопируйте `api_id` (числовой) и `api_hash` (строка)

## Шаг 2: Настроить .env файл

Добавьте в конец файла `.env`:

```env
# === Telethon Parser Configuration ===
PARSER_ENABLED=true
TELETHON_API_ID=12345678                    # ← Замените на ваш API ID
TELETHON_API_HASH=abcdef1234567890abcdef... # ← Замените на ваш API Hash
TELETHON_PHONE=+79001234567                 # ← Ваш номер телефона
TELETHON_SESSION_NAME=parser_session
```

**⚠️ Важно:**
- `TELETHON_API_ID` - это **числовое** значение (например, 12345678)
- `TELETHON_API_HASH` - это **длинная строка** (например, abcdef1234567890abcdef1234567890)
- `TELETHON_PHONE` - ваш номер в международном формате (+7...)

## Шаг 3: Применить миграцию БД

Создайте таблицу `parser_config`:

```bash
alembic upgrade head
```

Если увидите ошибку про уже существующую таблицу - это нормально, значит миграция уже применена.

## Шаг 4: Интегрировать парсер в bot.py (ПОКА НЕ СДЕЛАНО)

⚠️ **Парсер ещё не интегрирован в bot.py!**

Нужно добавить код запуска ParserIntegration. Есть два варианта:

### Вариант A: Минимальная интеграция (для быстрого теста)

Добавьте в `bot.py` после строки с инициализацией scheduler (примерно строка 240):

```python
# Инициализация парсера заявок (если включен)
parser_integration = None
if Config.PARSER_ENABLED:
    try:
        from app.services.parser_integration import ParserIntegration
        parser_integration = ParserIntegration(bot, db)
        await parser_integration.start()
        logger.info("✅ Парсер заявок запущен")
    except Exception as e:
        logger.error(f"❌ Ошибка запуска парсера: {e}", exc_info=True)
```

И в функции остановки (примерно строка 290, в `finally` блоке):

```python
# Остановка парсера
if parser_integration:
    try:
        await parser_integration.stop()
        logger.info("Парсер остановлен")
    except Exception as e:
        logger.error(f"Ошибка остановки парсера: {e}")
```

### Вариант B: Полная интеграция

См. подробные инструкции в `docs/BOT_PY_INTEGRATION.md`

## Шаг 5: Первый запуск (авторизация Telethon)

Запустите бота:

```bash
python bot.py
```

**При первом запуске Telethon запросит код авторизации:**

```
Please enter the code you received: 12345
```

1. Telegram отправит вам SMS с кодом подтверждения
2. Введите этот код в консоль (5 цифр)
3. После успешной авторизации создастся файл `parser_session.session`
4. **Следующие запуски не потребуют повторной авторизации**

⚠️ **Важно:** Файл `parser_session.session` уже добавлен в `.gitignore` - не коммитьте его!

## Шаг 6: Создать тестовую группу

1. Создайте новую группу в Telegram (например, "Test Parser Group")
2. Добавьте в неё вашего бота (найдите по username)
3. Дайте боту права администратора в группе

## Шаг 7: Установить ID группы для мониторинга

**Способ 1 (через пересылку):**
1. Перешлите любое сообщение из тестовой группы боту в личку
2. Ответьте на пересланное сообщение командой:
   ```
   /set_group
   ```

**Способ 2 (напрямую в группе):**
1. Напишите в тестовой группе команду:
   ```
   /set_group
   ```
2. Бот установит ID этой группы

**Способ 3 (через ID напрямую):**
1. Узнайте ID группы через @username_to_id_bot
2. Отправьте боту:
   ```
   /set_group -1001234567890
   ```

## Шаг 8: Проверить статус парсера

Отправьте команду:

```
/parser_status
```

Бот должен ответить:
```
📊 Статус парсера:

🟢 Парсер: Включён
📍 Группа: -1001234567890

⚙️ Конфигурация:
  API ID: 12345678
  Телефон: +79001234567
```

## Шаг 9: Протестировать парсинг

Отправьте тестовое сообщение в группу:

**Пример 1 (полное сообщение):**
```
С/м не крутит барабан
ул. Ленина 5, кв. 10
+79001234567
завтра к 14:00
```

**Пример 2 (минимальное сообщение):**
```
холод шумит
ул. Гагарина 12
```

**Пример 3 (однострочное):**
```
П/м течёт. ул. Пушкина 3-5. 89995551122
```

## Ожидаемое поведение

1. **Парсер распознает сообщение**
2. **Бот отправит сообщение с подтверждением:**
   ```
   📋 Распознана заявка:

   🔧 Техника: Стиральная машина
   ❗ Проблема: не крутит барабан
   📍 Адрес: ул. Ленина 5, кв. 10
   📱 Телефон: +79001234567
   🕐 Время: завтра к 14:00

   ❓ Создать заявку?

   [✅ Да] [❌ Нет]
   ```

3. **При нажатии "Да":**
   - Создаётся заявка в БД
   - Бот отправляет уведомление с номером заявки

4. **При нажатии "Нет":**
   - Заявка не создаётся
   - Данные удаляются из памяти

## Отладка проблем

### Парсер не запускается

**Проблема:** Ошибка при запуске бота
```
ModuleNotFoundError: No module named 'telethon'
```

**Решение:**
```bash
pip install telethon==1.34.0
```

---

**Проблема:** Ошибка конфигурации
```
ValueError: TELETHON_API_ID не установлен
```

**Решение:** Проверьте `.env` файл, убедитесь что все переменные заполнены

---

### Парсер не распознаёт сообщения

**Проблема:** Сообщения в группе игнорируются

**Проверьте:**
1. ID группы установлен через `/set_group`
2. Бот добавлен в группу
3. Парсер включён (`/parser_status` показывает "Включён")

**Смотрите логи:**
```bash
tail -f logs/bot.log | grep -i "parser\|telethon"
```

---

### Telethon запрашивает код при каждом запуске

**Проблема:** Session файл не сохраняется

**Решение:**
1. Проверьте что `parser_session.session` создался
2. Убедитесь что у процесса есть права на запись
3. Проверьте что `TELETHON_SESSION_NAME` не меняется

---

### Callback кнопки не работают

**Проблема:** При нажатии "Да"/"Нет" ничего не происходит

**Причина:** Обработчики callback не зарегистрированы в `bot.py`

**Решение:** См. `docs/BOT_PY_INTEGRATION.md` для полной интеграции

---

## Проверка покрытия тестами

Запустите unit-тесты:

```bash
python -m pytest tests/services/telegram_parser/ -v
```

Ожидаемый результат:
```
============================= 53 passed in 10.92s =============================
```

## Структура тестовых данных

Для тестирования используйте различные форматы:

### ✅ Должны распарситься успешно:

```
С/м Samsung не отжимает
Ленина 10-5
89001234567

п/м не сливает воду
Гагарина 12 кв 3

Холодильник LG шумит. ул. Мира 25. +79995551122

ТВ не включается
пр. Победы 18
завтра после обеда
```

### ❌ Должны вернуть ошибку (недостаточно данных):

```
С/м сломалась
(нет адреса)

Ленина 10
(нет описания проблемы)

(пустое сообщение)
```

## Следующие шаги

После успешного локального тестирования:

1. ✅ Убедитесь что все тесты проходят
2. ✅ Проверьте что заявки создаются корректно в БД
3. ✅ Протестируйте различные форматы сообщений
4. 📝 Обновите CHANGELOG.md
5. 🚀 Деплой на продакшн

## Полезные команды

```bash
# Проверить статус парсера
/parser_status

# Установить группу для мониторинга
/set_group

# Запустить тесты
python -m pytest tests/services/telegram_parser/ -v

# Посмотреть логи в реальном времени
tail -f logs/bot.log

# Применить миграции
alembic upgrade head

# Откатить миграцию (если нужно)
alembic downgrade -1
```

## Дополнительные ресурсы

- 📖 [docs/PARSER_FEATURE.md](PARSER_FEATURE.md) - Полное описание функции
- 🔧 [docs/BOT_PY_INTEGRATION.md](BOT_PY_INTEGRATION.md) - Инструкции по интеграции
- 📊 [docs/implementation/stage8_testing.md](implementation/stage8_testing.md) - Отчёт о тестировании
