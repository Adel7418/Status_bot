# Быстрый старт парсера (5 минут)

## ✅ Что уже сделано:

1. ✅ Парсер полностью написан и протестирован (53 unit-теста)
2. ✅ Интегрирован в bot.py
3. ✅ Все импорты работают корректно

## 🚀 Что нужно для запуска:

### 1. Добавить настройки в .env

Добавьте в конец файла `.env`:

```env
# Telethon Parser
PARSER_ENABLED=true
TELETHON_API_ID=12345678           # Ваш API ID с https://my.telegram.org
TELETHON_API_HASH=abcdef...        # Ваш API Hash
TELETHON_PHONE=+79001234567        # Ваш номер телефона
```

📝 **Где взять API credentials:**
- https://my.telegram.org → API development tools → Create application

### 2. Применить миграцию

```bash
alembic upgrade head
```

### 3. Запустить бота

```bash
python bot.py
```

**При первом запуске** Telethon попросит код из SMS - введите его в консоль.

### 4. Настроить группу

В вашей тестовой Telegram-группе:

```
/set_group
```

Или перешлите сообщение из группы боту и ответьте `/set_group`

### 5. Проверить статус

```
/parser_status
```

### 6. Отправить тестовое сообщение

Напишите в группу:

```
С/м не крутит барабан
ул. Ленина 5, кв. 10
+79001234567
завтра к 14:00
```

Бот должен ответить с подтверждением!

## 🔍 Проверка импортов

Чтобы убедиться что всё работает до запуска бота:

```bash
python test_parser_import.py
```

Ожидаемый результат:
```
[OK] Все импорты успешны!
[OK] Парсинг успешен!
[OK] Все проверки пройдены!
```

## 📚 Подробная документация

- `docs/PARSER_LOCAL_TESTING.md` - Полное руководство
- `docs/PARSER_FEATURE.md` - Описание функции
- `docs/BOT_PY_INTEGRATION.md` - Детали интеграции

## ❓ Проблемы?

**Парсер не запускается:**
- Проверьте что telethon установлен: `pip install telethon==1.34.0`
- Проверьте что в .env заполнены все переменные

**Сообщения не парсятся:**
- Убедитесь что `/set_group` выполнена
- Проверьте `/parser_status` - должно быть "Включён"

**Другие вопросы:**
- См. docs/PARSER_LOCAL_TESTING.md (раздел "Отладка проблем")
